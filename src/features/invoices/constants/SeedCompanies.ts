import type { Party } from '@/features/invoices/types';

/**
 * Customers a brand new registry starts with.
 *
 * Empty on purpose: the registry ships with no company data. Add the first
 * customer in Settings.
 */
export const SeedCompanies: Party[] = [];
